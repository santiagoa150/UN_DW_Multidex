/**
 * The `PokemonMovementDto` type defines the shape of the data transfer object (DTO) for a Pokémon movement.
 */
export type PokemonMovementDto = {
    pokemonId: number;
    name: string;
    levelLearnedAt: number;
};
