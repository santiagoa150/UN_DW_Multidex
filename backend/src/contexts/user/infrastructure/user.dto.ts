/**
 * The `UserDto` type defines the shape of the data transfer object (DTO) for a user.
 */
export type UserDto = {
    userId: string;
    email: string;
    username: string;
    password: string;
    names: string;
    lastNames: string;
};
