/**
 * Constants for the User HTTP API.
 */
export enum HttpUserConstants {
    PREFIX = 'user',
    API_TAG = 'User Operations',
    LOGIN_URI = 'login',
    SIGN_UP_URI = 'sign-up',
    GET_USER_URI = 'id',
}
