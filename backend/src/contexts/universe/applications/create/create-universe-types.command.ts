import { ICommand } from '@nestjs/cqrs';

/**
 * `CreateUniverseTypesCommand` is a command that represents the action to create universe types.
 */
export class CreateUniverseTypesCommand implements ICommand {}
