/**
 * Constants for Universe HTTP API.
 */
export enum HttpUniverseConstants {
    PREFIX = 'universe',
    API_TAG = 'Universe Operations',
    GET_UNIVERSE_ENTITY_BY_ID_AND_TYPE_URI = 'entity/by-id-and-type',
}